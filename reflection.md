# 💭 Reflection: Game Glitch Investigator

Answer each question in 3 to 5 sentences. Be specific and honest about what actually happened while you worked. This is about your process, not trying to sound perfect.

## 1. What was broken when you started?

- What did the game look like the first time you ran it?
- List at least two concrete bugs you noticed at the start  
  (for example: "the hints were backwards").

**Bug Reproduction Log**

Document at least 3 bugs you found. Add rows as needed.

| Input | Expected Behavior | Actual Behavior | Console Output / Error |
|-------|-------------------|-----------------|------------------------|
| `"12.9"` (decimal) | Reject decimal input | Silently truncated to `12` | None |
| Guess 101 (above secret) | Display "Too High" | Hint direction was inverted | None |
| Guess `100` when secret is `9` | Use numeric comparison (`100` > `9`) | Lexicographic comparison (`"100"` < `"9"`) | None |
| Empty/invalid guess (e.g., `"abc"`) | Don't consume attempt | Attempts incremented before validation | None |
| New Game after winning | Reset state (status/score/history) | State remained stale, locking player out | None |

---

## 2. How did you use AI as a teammate?

- Which AI tools did you use on this project (for example: ChatGPT, Gemini, Copilot)?

  - Claude with skills and plugins.

- Give one example of an AI suggestion that was correct (including what the AI suggested and how you verified the result).
  - Everytime I ask it to review some code and make it simmpler or maintainable, for some reason it works.
- Give one example of an AI suggestion that was incorrect or misleading (including what the AI suggested and how you verified the result).
  - Every time I ask for things that are not specific like subjective things that can be done in alot of ways

---

## 3. Debugging and testing your fixes

- How did you decide whether a bug was really fixed?
  - If I try to recreate the bugs and they cant be recreated.
- Describe at least one test you ran (manual or using pytest)  
  and what it showed you about your code.
  - Ran the code and try to repeat the error generated by the program. That is after the agent worked on getting the green ligth in the pytests created for each bug.
- Did AI help you design or understand any tests? How?
  - I asked the agent to design solutions using as without overcomplicating the code and making the solutions to the bugs more maintainable.

---

## 4. What did you learn about Streamlit and state?

- How would you explain Streamlit "reruns" and session state to a friend who has never used Streamlit?

---

## 5. Looking ahead: your developer habits

- What is one habit or strategy from this project that you want to reuse in future labs or projects?
  - This could be a testing habit, a prompting strategy, or a way you used Git.
- What is one thing you would do differently next time you work with AI on a coding task?
- In one or two sentences, describe how this project changed the way you think about AI generated code.
